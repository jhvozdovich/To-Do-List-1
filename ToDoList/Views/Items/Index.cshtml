@using ToDoList.Models;

<h1>To Do List</h1>
@if (Model.Count == 0)
{
  <p>Your list is empty! You must have something to do!</p>
}
<ul>
  @foreach (Item item in Model)
  {
  <li>@item.Description</li>
  }
  <a href="/items/new">Add a new item.</a>
</ul>